<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="yuan.icespring.openglesdemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TextureViewNativeDrawActivity"></activity>
        <activity android:name=".SurfaceViewNatvieDrawActivity" />
        <activity android:name=".TextureViewDrawActivity" />
        <activity android:name=".TextureViewDraw2Activity" />
        <activity android:name=".GLSurfaceViewNativeDrawActivity" />
        <activity android:name=".GLSurfaceViewDrawActivity" />
        <activity android:name=".GLSurfaceViewDrawTexurueActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>